# Extra ate-api ClusterRole/Binding only. Used by platform-substrate-rbac.
# Helm chart + SandboxConfig/gvisor-default live in platform/substrate-app
# (platform-substrate). Do not add helmCharts or SandboxConfig here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ate-api-server-extra-rbac.yaml
