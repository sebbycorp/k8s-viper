apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# ClusterSecretStore is applied after Vault init — see
# cluster-secret-store-vault.example.yaml and docs/vault-eso-setup.md
