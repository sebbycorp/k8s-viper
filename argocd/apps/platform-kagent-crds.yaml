apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-kagent-crds
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: viper
  source:
    # Argo CD 3.5 treats the OCI parent path as the artifact (GHCR 403).
    # Full chart URL + chart: matches the working agentgateway pattern.
    repoURL: oci://ghcr.io/kagent-dev/kagent/helm/kagent-crds
    chart: kagent-crds
    targetRevision: 0.10.0-rc2
    helm:
      releaseName: kagent-crds
  destination:
    server: https://kubernetes.default.svc
    namespace: kagent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
