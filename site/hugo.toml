baseURL = "https://sebbycorp.github.io/k8s-viper/"
languageCode = "en-us"
title = "k8s-viper"
enableRobotsTXT = true
disableKinds = ["taxonomy", "term", "RSS", "sitemap"]
relativeURLs = false
canonifyURLs = false

[params]
  description = "kagent SandboxAgents on Viper (gVisor, not plain Agents) plus the single-node k3s GitOps lab handbook."
  github = "https://github.com/sebbycorp/k8s-viper"
  defaultNodeIP = "172.16.10.135"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 3
    ordered = false

[outputs]
  home = ["HTML"]
  section = ["HTML"]
